#!/usr/bin/env sh

# Shows the usage
function usage() {
    cat << EOS
Usage:
  wifi-password [options] [network-name]
Options:
  -v, --version    Output version
  -h, --help       Show the usage.
Examples:
  wifi-password foo-network
EOS
}
